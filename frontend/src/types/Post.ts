// movie type for declaring var types
export type Post = {
  id: number;
  username: string;
  caption: string;
};

// moviecardProps type for declaring var types
export type PostProps = {
  post: Post;
};
